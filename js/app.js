(function (Flowtime) {
  'use strict';

  // Show Progress Bar
  Flowtime.showProgress(true);

  // Use Clicker
  Flowtime.clicker(true);

  Flowtime.setTransitionTime(350);

  Flowtime.start();

}(window.Flowtime));
